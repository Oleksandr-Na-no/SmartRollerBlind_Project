<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- <uses-permission android:name="android.permission.UPDATE_DEVICE_STATS" /> -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- <uses-permission android:name="android.permission.BIND_APPWIDGET" /> -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="sensorPortrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smart_Roller_Blind"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Alarm"
            android:exported="false" />
        <activity
            android:name=".QR_code"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Add_Code" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Share"
            android:exported="false" />
        <activity
            android:name=".Loading"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Name_add_Activity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Set_top_activity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Set_bottom_activity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Speed_Activity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Settings_Activity"
            android:exported="false"
            android:screenOrientation="portrait" /> <!-- <activity -->
        <!-- android:name=".Calibrate_Activity" -->
        <!-- android:exported="false" -->
        <!-- android:screenOrientation="portrait" /> -->
        <activity
            android:name=".Device_Activity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Add_device"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
            as we want to open main activity from our link so we are specifying
            only in main activity or we can specify that in different activity as well
            on below line we are adding intent filter to our MainActivity
            -->
            <intent-filter
                android:autoVerify="true"
                tools:targetApi="m">

                <!-- below line is to set the action to our intent to view -->
                <action android:name="android.intent.action.VIEW" />
                <!-- on below line we are adding a default category to our intent -->
                <category android:name="android.intent.category.DEFAULT" />
                <!-- on below line we are adding a category to make our app browsable -->
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                     on below line we are specifying the host name and
                    the scheme type from which we will be calling our app
                -->
                <data android:scheme="https" />
                <data
                    android:host="www.chaitanyamunje.com"
                    android:pathPrefix="/hello" />
            </intent-filter>
        </activity>
    </application>

</manifest>